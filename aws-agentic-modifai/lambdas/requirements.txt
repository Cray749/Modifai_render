# Shared dependencies for all pipeline Lambda functions
# Pin minor versions for reproducibility; update intentionally.

requests>=2.31.0,<3.0.0            # OpenRouter HTTP client (replaces google-genai)
boto3>=1.34.0,<2.0.0               # AWS SDK — S3, Secrets Manager
PyPDF2>=3.0.0,<4.0.0               # PDF text extraction (document_processor, intent_analyzer)
